# Dataset configuration for YOLO segmentation

# Paths
path: ./datasets  # dataset root dir
train: images/train  # train images (relative to 'path')
val: images/val    # val images 

# Classes
nc: 1  # number of classes
names: ['0']  # class names