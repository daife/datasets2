# Dataset configuration for YOLO segmentation

# Paths
path: ../dataset2  # dataset root dir
train: images/train  # train images (relative to 'path')
val: images/train    # val images (using train set for now, you can split later)

# Classes
nc: 1  # number of classes
names: ['0']  # class names